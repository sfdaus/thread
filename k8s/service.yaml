apiVersion: v1
kind: Service
metadata:
  name: thread-api
  namespace: prakarsa
spec:
  type: ClusterIP
  selector: { app: thread-api }
  ports:
    - name: http
      port: 80
      targetPort: 8080
